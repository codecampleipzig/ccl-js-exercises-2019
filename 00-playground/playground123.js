var drunkness = 0.005;
var age = 20

if (age >=18 && drunkness <= 0.005) {
    console.log("Get in");
   } else {
       console.log ("NOO!");
   }


   var favouriteCake = "cream cake";

   if (favouriteCake == "carrot cake") {
       console.log ("Lets bake a carrot cake");{
           
    else if (favoriteCake == "cheese cake") {
       console.log ("Let´s bake a cheese cake, we need dcheese."
    }

    console.log (process.argv);