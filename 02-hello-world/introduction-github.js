var myName = "Björn";
var myGithub = "https://github.com/burnilein";

function introduce(name, url) {
    console.log (`My name is ${name} and you can find me at ${url}.`);
}

introduce(myName, myGithub);