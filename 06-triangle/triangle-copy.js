var depth = 10;

for (i = 0; i < depth; i++) {
    var line = ""
            
        line = line + "#";
    }
        var pattern = ""
        for (j = -4; j < depth; j++) {

     pattern = pattern + 'xx';   }
      
    
    console.log (pattern + line);
