var array = ['a', 'b', 'c'];

array[0]

array.length
array['length']

"Hello".length
"Hello"[1]  // 'e'

var color = {
   red : 123,
   green : 43,
   blue: 1
};

color['red'] // 123
color.red    // 123

// Mutability

color.red = 12;
array[1] = 'B';

// Methods
console.log ("Hello World");

Math.max (1, 2, 3);

array.push ('d');
array.pop();     // 'd'